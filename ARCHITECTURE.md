# M2A 에고그램 리포트 자동화 — 시스템 구조

> **현재 빌드: v0.7** (5/25 기록됨) — 손소장 26.0525 수정 5건 + 리포트 정리. 역할별 안전구간 테이블(`SAFE_RANGES`), 컨설턴트 CM6 공통 3섹션(`cm6_common_consultant.yaml`), "코칭"→"조율" 라벨, 결과화면 총점·재시도 삭제.
> 이전 안정 빌드: v0.5 (`_dev/mind2action/egogram-v0.5/`)

> **5/18 회의 반영** — 리포트 렌더링 자리에서 §1·§2 통합(CM1 짧은 한 줄 제거, CM2를 §1 점수 아래로), CM4-3 some_coaching 안내 멘트 폐기, CM4-4 해당 ego는 CM4-1 대신 CM4-4로 대치, CM8 명언 폐기, 마무리(드리고 싶은 말씀) → 한두 줄 인사 + 연락처. 데이터 계층(cm_*.yaml)은 보존, 렌더 단계에서 차단.

## 전체 데이터 흐름

```
[설문 수집]              [CM 데이터]              [리포트 생성]
구글 설문 50문항    →    점수 엔진         →     CM 조회 엔진    →    HTML 렌더링    →    PDF
(CP/NP/A/FC/AC         (구간 분류,              (점수→텍스트          (템플릿 +            출력
 각 10문항)             TOP/BOTTOM)              매칭)                 그래프)
```

## 구성 요소

### 1. 데이터 계층

**CM 데이터 (Phase 0에서 생성)**

엑셀 3종 → JSON 정규화. 세 직군별 CM 테이블:

| CM | 내용 | 조회 키 | 보험설계사 | 관리자 | 코치 |
|----|------|---------|-----------|--------|------|
| CM1 | 성향 설명 | 243유형 (3^5) | O | O (축소) | O |
| CM2 | 성향과 말투 | 점수구간 (A/B/C × 5) | O | O | O |
| CM3 | 강점 | TOP 성향 | O | O | O |
| CM4 | 코칭 포인트 | TOP/BOTTOM 조합 + 세밀 조건 | O (4-1~4-4) | O (4-1~4-3, 4-3 확장) | O |
| CM5 | 화법 가이드 | 점수구간 | O (보장제안) | O (성과화법) | O (코칭화법) |
| CM6 | 거절/대응 | 점수구간 | O (고객거절) | O | O |
| CM7 | 리크루팅/신인 | 점수구간 | O | O | O (신인코칭) |
| CM8 | 명언 | TBD (고정/랜덤) | O | - | - |

**설문 데이터**

- 구글 설문 취합: 63컬럼 (기본정보 8 + 문항 50 + 계산점수 5)
- 342명 샘플 데이터 존재 (검증용)

```
data/
├── cm_insurance.json     # 보험설계사 CM1~CM8
├── cm_manager.json       # 관리자 CM1~CM7
├── cm_coach.json         # 코치/멘토 CM1~CM7
├── success_patterns.json # 직군별 성공구간 평균치
└── sample_responses.csv  # 검증용 설문 데이터 (342명)
```

### 2. 점수 엔진 (Phase 1)

```
입력: 5개 원점수 (CP, NP, A, FC, AC)
  ↓
구간 분류: A급(14+) / B급(8~13) / C급(7-)
  ↓
TOP/BOTTOM 결정: 최고점 자아상태 / 최저점 자아상태
  ↓
조건 플래그 생성: CM4 세밀한 코칭용 복합 조건
  ↓
출력: ScoreResult { scores, grades, top, bottom, flags }
```

**점수 구간 정의** (보험설계사 기준):
- A급: 14점 이상 — 성공구간 (긍정적 해석)
- B급: 8~13점 — 보통구간 (중립적 해석 + 개선 방향)
- C급: 7점 이하 — 주의구간 (적극적 코칭 포인트)

### 3. CM 조회 엔진 (Phase 2)

```
입력: ScoreResult + JSON CM 데이터
  ↓
직군 분기: insurance / manager / coach
  ↓
CM1~CM8 순차 조회:
  CM1: 5개 구간 조합 → 243유형 중 1개 텍스트
  CM2: 각 자아상태 구간별 텍스트 5건
  CM3: TOP 성향 기반 강점 텍스트
  CM4: TOP×BOTTOM 조합 + 세밀 조건 → 코칭 포인트
  CM5~CM7: 직군별 특화 텍스트
  CM8: 명언 (보험설계사만)
  ↓
출력: ReportContent { sections: { cm1: string, cm2: string[], ... } }
```

### 4. HTML 렌더링 (Phase 3)

**리포트 구조 (보험설계사 11p 기준)**:

```
p1  : 표지 — 성향 코칭 리포트 목적
p2  : OOO님의 성향 (CM1)
p3-4: 자아상태의 성향과 말투 (CM2) + 성공구간 비교 그래프
p5  : 내 성향의 강점 (CM3)
p6-8: 내 성향의 코칭 포인트 (CM4-1~4-4)
p9  : 보장 제안 시 화법 (CM5) + 꿀TIP
p10 : 클로징 전 고객 거절 시 (CM6)
p11 : 신입 리크루팅 레벨업 (CM7) + 명언 (CM8)
```

**그래프 요소**:
- 바 차트: 본인 5개 점수 vs 성공구간 평균 (나란히 비교)
- (선택) 레이더 차트: 5각형 분포

**기술 선택지**:
- 그래프: Chart.js 또는 SVG 직접 생성
- 템플릿: 바닐라 HTML + JS 문자열 치환

### 5. PDF 변환 (Phase 4)

**후보 기술**:

| 옵션 | 장점 | 단점 |
|------|------|------|
| html2pdf.js | 클라이언트 사이드, 설치 불필요 | 페이지 나눔 제어 약함 |
| Puppeteer | 정밀한 렌더링, 페이지 제어 | Node.js 서버 필요 |
| WeasyPrint | Python, CSS Paged Media 지원 | Python 의존성 |

→ 최종 결정은 Phase 3 완료 후 렌더링 복잡도 보고 판단.

---

## 인프라 구조

### 현재 (Phase 0~4, 로컬 개발)

```
mind2action/
├── ROADMAP.md
├── ARCHITECTURE.md
├── .gitignore
├── data/                  # JSON CM 데이터 (Phase 0 산출물)
├── src/
│   ├── score_engine.js    # 점수 엔진 (Phase 1)
│   ├── cm_lookup.js       # CM 조회 엔진 (Phase 2)
│   └── report_render.js   # HTML 렌더링 (Phase 3)
├── templates/
│   ├── insurance.html     # 보험설계사 리포트 템플릿
│   ├── manager.html       # 관리자 리포트 템플릿
│   └── coach.html         # 코치/멘토 리포트 템플릿
├── scripts/
│   └── xlsx_to_json.js    # 엑셀→JSON 변환 (Phase 0)
└── tests/
    └── ...
```

### 추후 (서비스화)

```
[사용자]
  ↓ 웹 설문 (자체 플랫폼, 구글 폼 탈피)
  ↓
[프론트엔드] 바닐라 HTML/JS
  ├── 설문 입력
  ├── 리포트 열람 (로그인 필요)
  └── 관리자 대시보드
  ↓
[인증] Supabase Auth
  ├── 관리자 역할: 리포트 생성, CM 관리, 배치 처리
  └── 사용자 역할: 본인 리포트 열람
  ↓
[데이터] Supabase DB (JSON → PostgreSQL 마이그레이션)
  ↓
[배포] GitHub Pages + 커스텀 도메인 (M2A 도메인)
```

---

## 코드 원칙

- **단일 HTML 또는 최소 파일 분리** (피터공 개발 스타일)
- **바닐라 JS**, 프레임워크 없음
- **한국어 우선**: UI, 주석, 변수명 설명 모두 한국어 OK
- **디자인**: 흰 배경, 미니멀, 블랙&화이트 기본

---

## 참고 문서

- `_init/TM_에고그램.md` — 프로젝트 전체 맥락, 팀, 사업 모델
- `에고그램_리포트 템플릿` — CM 참조 로직 pseudo-code (볼트 노트)
- `Assets/incoming/에고그램/리포트 구성_보험설계사.pdf` — 원본 리포트 시안
- `26.0313 M2A 회식 — 에고그램 사업 대화` — 소득별 톤 변형 요구사항
